"""REMOVED — see ADR-003.

The Lizard model was consolidated into the unified `Animal` model
(app/models/animal.py) as of the anm_20260514 migration. This file is
intentionally empty and should be `git rm`'d — it's kept only because
the dev sandbox couldn't delete it directly.
"""
