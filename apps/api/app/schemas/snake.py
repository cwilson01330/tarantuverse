"""REMOVED — see ADR-003.

Snake schemas were consolidated into app/schemas/animal.py as of the
anm_20260514 migration. This file is intentionally empty and should be
`git rm`'d — kept only because the dev sandbox couldn't delete it.
"""
