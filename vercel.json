{
  "version": 2,
  "functions": {
    "apps/web/**": {
      "runtime": "nodejs20.x"
    }
  },
  "buildCommand": "cd apps/web && rm -rf .next node_modules && pnpm install --no-frozen-lockfile && node build-workaround.js",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install -g pnpm@latest && cd apps/web && pnpm install --no-frozen-lockfile",
  "framework": null
}
